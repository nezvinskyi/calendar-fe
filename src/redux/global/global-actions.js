import { createAction } from '@reduxjs/toolkit';

export const openModalAddEvent = createAction('global/openModalAddEvent');
export const closeModalAddEvent = createAction('global/closeModalAddEvent');
